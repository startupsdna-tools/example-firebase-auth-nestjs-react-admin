/**
 * Key for the admin auth public metadata
 */
export const AUTH_PUBLIC = 'auth-public';

/**
 * Name of the session cookie
 */
export const SESSION_COOKIE_NAME = '__session';

/**
 * Key for the admin auth options provider
 */
export const FIREBASE_AUTH_OPTIONS = Symbol('FIREBASE_AUTH_OPTIONS');
